# Base URL of the NervaAPI instance the documentation describes.
# Used to render the curl examples and any links in the docs.
# Set this to your deployed API origin in production.
VITE_API_BASE_URL=http://localhost:5000
